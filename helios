#!/usr/bin/env python
from typing import IO
import nodes
from nodes import *
from api import buildBuiltins
import sys, os

MAX_F = 100


def dprint(v, tok, end="\n", file=sys.stderr):
    if isinstance(tok, Intrinsic) and tok.name == "TOGGLE_DEBUG_OUTPUT":
        return
    if nodes.strace:
        print(v, end=end, file=file)


class Frame:
    def __init__(self, parent=None):
        self.parent = parent
        self.vars = {}
        self._stack = []
        if parent:
            self._stack = parent._stack  # Shared stack logic

    def push(self, value):
        self._stack.append(value)

    def pop(self):
        if not self._stack:
            raise ValueError("Stack underflow")
        x = self._stack.pop()
        return x

    def top(self):
        if not self._stack:
            raise ValueError("Stack underflow")
        x = self._stack[-1]
        return x

    def set(self, name, value):
        self.vars[name] = value

    def get(self, name):
        if name in ["true", "false"]:
            return name == "true"
        if name in self.vars:
            return self.vars[name]
        if self.parent:
            return self.parent.get(name)
        raise ValueError(f"{name} is not defined")


class Vm:
    def __init__(self, filename):
        with open(filename, "r") as f:
            raw_tokens = tokenizer.tokenize(f.read(), f.name)

        # Phase 1: Parsing
        parser = Parser(raw_tokens)
        self.functions = parser.parse_program()

        # Phase 2: Runtime Setup
        self.frames = [Frame()]
        self.funcs = {}

    def run_function(self, fn_def: FunctionDef):
        # Bind Arguments
        # Note: In stack languages, args are usually popped in reverse order
        for arg_name in reversed(fn_def.args):
            val = self.frames[-1].pop()
            self.frames[-1].set(arg_name, val)

        self.execute_block(fn_def.body)

    def execute_block(self, block: Block):
        for node in block.statements:
            try:
                if not self.frames:
                    break  # handled 'done'
                before = self.frames[-1]._stack.copy()
                self.execute_node(node)
                after = self.frames[-1]._stack
                dprint("    - " * (len(self.frames) - 1), node, end="", file=sys.stderr)
                dprint(f"{node} :: {before} -> {after}", node, file=sys.stderr)
            except Return:
                break

    def execute_node(self, node: ASTNode):
        frame = self.frames[-1]

        if isinstance(node, Literal):
            frame.push(node.value)

        elif isinstance(node, Identifier):
            if node.name in self.functions:
                if len(self.frames) >= MAX_F:
                    raise ValueError("Stack overflow")

                new_frame = Frame(frame)
                self.frames.append(new_frame)
                dprint(" " * (len(self.frames) - 1), node, end="", file=sys.stderr)
                dprint(">>" + node.name, node, file=sys.stderr)
                self.run_function(self.functions[node.name])
                dprint(" " * (len(self.frames) - 1), node, end="", file=sys.stderr)
                dprint("<<" + node.name, node, file=sys.stderr)
                self.frames.pop()
            # Check for Variables
            else:
                val = frame.get(node.name)
                frame.push(val)

        elif isinstance(node, Intrinsic):
            if node.name == "pop":
                frame.pop()
            elif node.name == "top":
                size = len(frame._stack)
                frame.push(size)
            elif node.name == "return":
                raise Return()
            elif node.name == "break":
                raise Break()
            elif node.name == "continue":
                raise Continue()
            elif node.name == "DEBUG_ACTIVE":
                frame.push(nodes.strace)
            elif node.name == "done":
                self.frames.clear()  # Hard stop
            elif node.name == "TOGGLE_DEBUG_OUTPUT":
                nodes.strace = not nodes.strace

        elif isinstance(node, If):
            cond = frame.pop()
            if cond:
                self.execute_block(node.body)
            elif node.else_block:
                self.execute_block(node.else_block)

        elif isinstance(node, Op):
            b = frame.pop()
            a = frame.pop()
            sb = {
                "+": lambda a, b: a + b,
                "-": lambda a, b: a - b,
                "/": lambda a, b: a / b,
                "*": lambda a, b: a * b,
                "==": lambda a, b: a == b,
                "!=": lambda a, b: a != b,
                ">": lambda a, b: a > b,
                "<": lambda a, b: a < b,
            }
            frame.push(sb[node.symbol](a, b))

        elif isinstance(node, Attribute):
            conversions = {
                "str": str,
                "int": int,
                "bool": bool,
                "float": float,
                "type": lambda x: type(x).__name__,
            }
            if node.name in conversions:
                frame.push(conversions[node.name](frame.pop()))
            else:
                self.funcs[node.name](frame)

        elif isinstance(node, Repeat):
            count = frame.pop()
            if not isinstance(count, int):
                raise ValueError(f"Repeat count must be int, got {type(count)}")
            for _ in range(count):
                try:
                    self.execute_block(node.body)
                except Break:
                    break
                except Continue:
                    continue

        elif isinstance(node, While):
            while True:
                try:
                    self.execute_block(node.body)
                except Break:
                    break
                except Continue:
                    continue
                v = frame.pop()
                if not v:
                    break

        elif isinstance(node, MultiSet):
            # [ a, b ] pops b, then a
            # The list in AST is [a, b].
            # Stack top is 'b value', then 'a value'.

            # We need to assign right-to-left based on stack order
            for name in reversed(node.names):
                frame.set(name, frame.pop())

    def run(self):
        if "main" not in self.functions:
            raise ValueError("No main function defined")

        # Create a frame for main and run it
        self.run_function(self.functions["main"])


if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("Usage: ./helios.py <file>")
        sys.exit(1)
    sys.path.insert(0, os.path.dirname(os.path.realpath(sys.argv[1])))
    vm = Vm(sys.argv[1])
    sys.argv.reverse()
    sys.argv.pop()
    buildBuiltins(vm)
    vm.run()
